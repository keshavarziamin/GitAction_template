#include <vector>
#include <utility>

    class Sort
    {
    private:
        /* data */
    public:
        Sort(/* args */){};
        ~Sort(){};

        void sort(std::vector<int> &list);
    };
    

