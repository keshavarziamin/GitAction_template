#include <vector>

namespace Sort
{

    class Sort
    {
    private:
        /* data */
    public:
        Sort(/* args */){};
        ~Sort(){};
        virtual void sort(std::vector<int> &list);
    };

}